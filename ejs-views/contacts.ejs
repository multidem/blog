<!DOCTYPE html>
<html lang="en">

  <%- include('./parts/head.ejs') %>

<body>
  <%- include('./parts/nav.ejs') %>
<main>
  <h1>Наши контакты</h1>
  <ul>
    <% if (ourContacts.length) { %>
      <% ourContacts.forEach(({ link, name }) => { %>
        <li>
          <a href="<%= link %>" target="_blank"><%= name %></a>
        </li>
      <% }) %>
    <% } %>
  </ul>
</main>
</body>

</html>